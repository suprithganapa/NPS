server:
  ip: "0.0.0.0"
  iface: "eth0"

auth:
  totp_secret: "JBSWY3DPEHPK3PXP"
  window_seconds: 60
  ttl_auth_seconds: 300

channels:
  jitter:
    enabled: true
    t0_ms: 20
    delta_ms: 5
    tau_ms: 2
    # stealth mode (blend with OS ping): t0_ms: 1000, delta_ms: 50, tau_ms: 25
  header:
    enabled: false
    lfsr_seed: 44257
    windows_compat: false
  dns:
    enabled: false

fec:
  enabled: true
  n: 32
  k: 28

drm:
  enabled: true
  fragment_bits: 32

targets:
  allowlist: []

firewall:
  mode: "log_only"
